<style>
    .form-control{

    }
</style>
{{ form_start(searchForm) }}
<div class="form-group-sm">
    <div class="input-group">
        <div class="form-floating">
            {{ form_widget(searchForm.search, {'attr': {'class': 'form-control input-sm', 'id': 'search', 'placeholder': 'Article title | description'}}) }}
            <label for="search_articles_admin[search]" style="color: #a3a4cc">Search</label>
        </div>
        <div class="form-floating">
            {{ form_widget(searchForm.date_publication, {'attr': {'class': 'form-control input-sm', 'placeholder': 'Date de publication'}}) }}
            <label for="search_articles_admin[search]" style="color: #a3a4cc">Date de publication</label>
        </div>


        {{ form_widget(searchForm.submit, {'attr': {'class': 'btn btn-label-primary btn-sm'}}) }}
    </div>

</div>
{{ form_end(searchForm) }}
