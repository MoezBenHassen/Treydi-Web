{{ form_start(searchForm) }}
<div class="form-group">
    <div class="input-group">
        {{ form_widget(searchForm.search) }}
        {{ form_widget(searchForm.date_publication) }}
        {{ form_widget(searchForm.submit) }}
    </div>

</div>
{{ form_end(searchForm) }}