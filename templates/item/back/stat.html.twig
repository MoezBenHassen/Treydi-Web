{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}Items
{% endblock %}

{% block stylesheets %}
	<!-- Fonts -->
	<link rel="preconnect" href="https://fonts.googleapis.com/">
	<link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin>
	<link
	href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&amp;display=swap" rel="stylesheet">

	<!-- Icons -->
	<link rel="stylesheet" href="{{ asset('assets/vendor/fonts/boxicons.css') }}"/>
	<link rel="stylesheet" href="{{ asset('assets/vendor/fonts/fontawesome.css') }}"/>
	<link
	rel="stylesheet" href="{{ asset('assets/vendor/fonts/flag-icons.css') }}"/>
	<!-- Core CSS -->
	<link rel="stylesheet" href="{{ asset('assets/vendor/css/rtl/core-dark.css') }}" class="template-customizer-core-css"/>
	<link rel="stylesheet" href="{{ asset('assets/vendor/css/rtl/theme-default-dark.css') }}" class="template-customizer-theme-css"/>
	<link
	rel="stylesheet" href="{{ asset('assets/css/demo.css') }}"/>

	<!-- Vendors CSS -->
	<style>
		.table-responsive {
			overflow-x: auto;
		}
		/* custom css that makes the table cell wrap if the text exceeds the cell length*/
		.table-responsive td {
			white-space: normal;
			text-align: justify;
		}
		::-moz-selection {
			/* Code for Firefox */
			background: yellow;
		}

		::selection {
			background: yellow;
		}
		#chose {
			width: 25px;

		}

		/*make the card the width of the screen available*/
	</style>
{% endblock %}


{% block javascripts %}
	<script src="{{ asset('assets/vendor/js/helpers.js') }}"></script>

	<!--! Template customizer & Theme config files MUST be included after core stylesheets and helpers.js in the <head> section -->
	<!--? Template customizer: To hide customizer set displayCustomizer value false in config.js.  -->
	<script src="{{ asset('public/assets/vendor/js/template-customizer.js') }}"></script>
	<!--? Config:  Mandatory theme config file contain global vars & default theme options, Set your preferred theme option in this file.  -->
	<script src="{{ asset('assets/js/config.js') }}"></script>
	<script></script>
{% endblock %}
{% block body %}

	<!-- Google Tag Manager (noscript) (Default ThemeSelection: GTM-5DDHKGP, PixInvent: GTM-5J3LMKC) -->
	<noscript>
		<iframe src="https://www.googletagmanager.com/ns.html?id=GTM-5DDHKGP" height="0" width="0" style="display: none; visibility: hidden"></iframe>
	</noscript>
	<!-- End Google Tag Manager (noscript) -->

	<!-- Layout wrapper -->
	<div class="layout-wrapper layout-content-navbar  ">
		<div
			class="layout-container">
			<!-- Menu -->
			<aside id="layout-menu" class="layout-menu menu-vertical menu bg-menu-theme">
				{{ include('Dashboard_components/_menuDashboard.html.twig') }}
			</aside>

			<!-- / Menu -->
			<!-- Layout container -->
			<div
				class="layout-page">

				<!-- Navbar -->
				{{ include('Dashboard_components/_navbarDashboard.html.twig') }}
				<!-- / Navbar -->
				<br>
				<!-- Content wrapper -->
				<div
					class="content-wrapper">
					<!-- Content -->

					<div
						class="container-fluid">
						<!-- Content -->

						<div class="card">
							<h4 class="card-header">Quantite</h4>
							<div class="table-responsive text-nowrap"></div>
							<div class="card">
								<h5 class="card-header">Nombre des items total : {{list | length }}</h5>
								<h5 class="card-header">Nombre des categories total : {{listc | length }}</h5>
								<h5 class="card-header">Nombre des items disponible : {{list|filter(object => object.isArchived() == 0) | length }}</h5>
								<h5 class="card-header">Nombre des categories disponible : {{listc|filter(object => object.isArchived() == 0) | length }}</h5>
								<h5 class="card-header">Nombre des items archivees : {{list|filter(object => object.isArchived() == 1) | length }}</h5>
								<h5 class="card-header">Nombre des categories archivees : {{list|filter(object => object.isArchived() == 0) | length }}</h5>
								<h5 class="card-header">Nombre des items dans echange : {{list|filter(object => object.getIdEchange() != null) | length }}</h5>
								<h5 class="card-header">Nombre des items physique : {{list|filter(object => object.getType() == "Physique") | length }}</h5>
								<h5 class="card-header">Nombre des items physique neuf : {{list|filter(object => object.getEtat() == "Neuf") | length }}</h5>
								<h5 class="card-header">Nombre des items physique occasion : {{list|filter(object => object.getEtat() == "Occasion") | length }}</h5>
								<h5 class="card-header">Nombre des items virtuelle : {{list|filter(object => object.getType() == "Virtuelle") | length }}</h5>
								<h5 class="card-header">Nombre des items service : {{list|filter(object => object.getType() == "Service") | length }}</h5>
								<h5 class="card-header">Nombre total des vues :</h5>
								<h5 class="card-header">Nombre total des likes :</h5>
								<h5 class="card-header">Nombre total des dislikes :</h5>
							</div>
						</div>
<br>
										<div class="card">
							<h4 class="card-header">Top</h4>
							<div class="table-responsive text-nowrap"></div>
							<div class="card">
								<h5 class="card-header">La categorie avec le plus d'items :</h5>
								<h5 class="card-header">La catégorie avec le plus vues :</h5>
								<h5 class="card-header">La catégorie avec le plus likes :</h5>
								<h5 class="card-header">La catégorie avec le plus dislikes :</h5>
								<h5 class="card-header">L'item avec le plus vues :</h5>
								<h5 class="card-header">L'item avec le plus likes :</h5>
								<h5 class="card-header">L'item avec le plus dislikes :</h5>
							</div>
						</div>


					</div>

				</div>
			</div>
		</div>
	</div>
	<!-- Core JS -->
	<!-- build:js assets/vendor/js/core.js -->
	<script src="{{ asset('/assets/vendor/libs/jquery/jquery.js') }}"></script>

	<script src="{{ asset('/assets/vendor/libs/popper/popper.js') }}"></script>

	<script src="{{ asset('/assets/vendor/js/bootstrap.js') }}"></script>

	<script src="{{ asset('/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.js') }}"></script>

	<script src="{{ asset('/assets/vendor/libs/hammer/hammer.js') }}"></script>

	<script src="{{ asset('/assets/vendor/libs/i18n/i18n.js') }}"></script>

	<script src="{{ asset('/assets/vendor/libs/typeahead-js/typeahead.js') }}"></script>

	<script src="{{ asset('/assets/vendor/js/menu.js') }}"></script>

	<!-- endbuild -->

	<!-- Vendors JS -->

	<!-- Vendors JS -->

	<!-- Main JS --><script src="{{asset('/assets/js/maindash.js') }}"> </script>

	<!-- Page JS -->

	<script></script>
</body>{% endblock %}
