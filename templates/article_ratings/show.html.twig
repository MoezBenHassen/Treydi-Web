{% extends 'base.html.twig' %}

{% block title %}ArticleRatings{% endblock %}

{% block body %}
    <h1>ArticleRatings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_rating.id }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ article_rating.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_ratings_index') }}">back to list</a>

    <a href="{{ path('app_article_ratings_edit', {'id': article_rating.id}) }}">edit</a>

    {{ include('article_ratings/_delete_form.html.twig') }}
{% endblock %}
