{{ form_start(form2) }}
<div
    class="mb-3">

    <!--star rating-->

    <style>
/*        .ratingStar {
            color: gray;
            font-size: 2em;
            cursor: pointer;
        }
        .grayStar {
            color: gray !important;
            font-size: 2em;
            cursor: pointer;
        }
        .pinkStar {
            color: red !important;
            font-size: 2em;
            cursor: pointer;
        }*/
    </style>
    <div style="border: 1px black solid">
        <span class="rating" data-rating="0">
            {#PRINT FIVE STAR WITH A FOR LOOP EACH STAR HAVING DATA NOTE AS THE LOOP INDEX#}
            {% for i in 1..5 %}
                {#if i < article.avgRating then the star has a 'low' class#}
                {% if i < article.avgRating %}
                    <i class="star hover" data-checked="true" data-note="{{ i }}">&#9733;</i>
                {% else %}
                    <i class="star " data-checked="false" data-note="{{ i }}">&#9733;</i>
                {% endif %}
            {% endfor %}
 {#           <i class="star ratingStar" data-checked="false" data-note="1">&#9733;</i>
            <i class="star ratingStar" data-checked="false" data-note="2">&#9733;</i>
            <i class="star ratingStar" data-checked="false" data-note="3">&#9733;</i>
            <i class="star ratingStar" data-checked="false" data-note="4">&#9733;</i>
            <i class="star ratingStar" data-checked="false" data-note="5">&#9733;</i>#}
        </span>
        <span>
            <i class="note">Note: 0</i>
        </span>
    </div>


    {#form widget form rating with disabled attribute#}

</div>
{#form2.rating form widget with input value being article.avgRating#}
{{ form_widget(form2.rating, {'attr': {'value': article.avgRating}}) }}

<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form2) }}
