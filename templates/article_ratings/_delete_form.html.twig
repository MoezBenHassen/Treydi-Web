<form method="post" action="{{ path('app_article_ratings_delete', {'id': article_rating.id}) }}" onsubmit="return confirm('Are you sure you want to delete this rating?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_rating.id) }}">
    <button class="btn">Delete</button>
</form>
